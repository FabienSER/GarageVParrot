<form method="post" action="{{ path('app_list_comment_delete', {'id': list_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ list_comment.id) }}">
    <button class="btn">Delete</button>
</form>
